# What I Need to Do

一款基于四象限法则的 macOS 桌面待办应用：常驻系统托盘、快捷键随时呼出，未完成的事项不会在第二天消失，而是自动延续并标注「已拖 N 天」。


![四象限网格视图](<img width="1184" height="1506" alt="demo" src="https://github.com/user-attachments/assets/38ce8371-73cb-40f2-9afa-ad0d4c6bbfe0" />)

*网格视图：四个象限并列，一眼看清精力分布*

---

## 一、为什么做这个

日常使用主流待办工具时，我反复遇到两个问题：

**痛点一：未完成的任务在次日「消失」。**
多数「今日待办」类应用以自然日为边界，今天没做完的事第二天不会主动出现在视野里，只能靠自己回忆或翻历史记录。结果是——真正被拖延的事，恰恰是最容易被系统遗忘的事。

**痛点二：所有任务被平铺成一份清单，缺少优先级。**
「回复一封邮件」和「完成季度方案」在列表里长得一模一样。缺少「重要 / 紧急」两个维度的区分，人天然会先做容易的、急的，把真正重要但不紧急的事一再往后推。

## 二、这个应用如何解决

| 痛点 | 解决方案 |
| --- | --- |
| 任务没有优先级 | 采用**艾森豪威尔四象限**（重要·紧急 / 重要·不急 / 急·不重要 / 都不是）分类，网格视图直接把四个象限并列呈现，一眼看清精力该放在哪里 |
| 重要的事被急事挤掉 | 列表视图按「未完成优先 → 象限序号 → 拖延天数」自动排序，优先级最高的任务永远在最上方 |
| 未完成任务次日消失 | 每条任务记录首次创建日期（`firstDate`）。未完成的任务**自动延续到今天**，并显示**「已拖 N 天」**标签；超过 3 天会高亮为警示色。已完成的任务当天归档，次日自动从视图中移除，保持界面清爽 |

核心设计原则：**完成的事会走，没完成的事不会走。** 拖延不再是无声的，而是被明确计数并呈现出来。

## 三、技术栈

- **前端**：原生 HTML / CSS / JavaScript，无框架、无构建步骤，全部逻辑与样式集中在单文件 `src/index.html`
- **数据**：`localStorage` 本地持久化，无后端、无账号、数据不出本机
- **桌面端**：[Tauri v2](https://tauri.app)（Rust）打包为 macOS 原生应用（`.app` / `.dmg`），相比 Electron 体积更小、内存占用更低
- **系统集成**（`src-tauri/src/main.rs`）：
  - 系统托盘常驻：左键单击显示 / 隐藏窗口，右键弹出菜单
  - 全局快捷键 `⌘ + Shift + T`：在任意应用中呼出或收起窗口
  - 点击窗口关闭按钮时隐藏到托盘而非退出，符合常驻工具的使用直觉

> 代码同时兼容 Windows（`Win + Shift + T`，产出 `.msi` / `.exe`），但主要在 macOS 上开发与验证。

## 四、主要功能

- **四象限分类**：添加时选择象限，或在输入框以 `!1` ~ `!4` 开头快速归类
- **双视图切换**：网格视图（四象限全局概览）与列表视图（按优先级线性排序），视图偏好本地记忆
- **拖延可视化**：未完成任务自动跨日延续，实时显示「已拖 N 天」，≥ 3 天高亮提醒
- **快速编辑**：点击彩色数字循环切换象限；双击标题或点击 ✎ 就地改名；✕ 删除
- **键盘优先**：输入框自动聚焦，回车连续添加，编辑态 `Enter` 保存 / `Esc` 取消
- **每日归档**：已完成任务当天保留可查、次日自动隐藏
- **状态提示**：顶栏根据剩余任务数量给出即时反馈
- **系统托盘 + 全局快捷键**：随时呼出，不打断当前工作流

## 五、如何运行

**环境要求**：Node.js（LTS）、Rust（[rustup.rs](https://rustup.rs)）、Xcode 命令行工具（`xcode-select --install`）。

```bash
# 1. 安装依赖
npm install

# 2. 开发模式运行（首次编译 Rust 需要几分钟，之后为增量编译）
npm run dev

# 3. 打包为 macOS 应用
npm run build
```

打包产物位于 `src-tauri/target/release/bundle/`（macOS 下为 `.dmg` 与 `.app`）。

如需替换应用图标，将源图放在根目录后执行 `npm run icon`，会自动生成各尺寸图标。

## 六、目录结构

```
today-todo/
├─ package.json              # 脚本与 Tauri CLI 依赖
├─ src/
│  └─ index.html             # 前端全部实现（UI + 逻辑 + 样式）
└─ src-tauri/
   ├─ Cargo.toml             # Rust 依赖
   ├─ tauri.conf.json        # 窗口与打包配置
   ├─ capabilities/          # 权限声明
   ├─ icons/                 # 应用图标
   └─ src/
      └─ main.rs             # 托盘、全局快捷键、关闭即隐藏
```
## 七、这个项目是怎么做出来的

我不是工程背景出身，本科法学，硕士读的也是法律。这个应用是我用 Claude Code 从零构建的，从第一版需求描述到最后打包出 .dmg，全程由我一个人推进。
把这一节写出来，是因为「用 AI 编程工具做出一个能用的东西」和「会写代码」是两件不同的事，我不想让读到这里的人产生误解。
我做的部分
定义问题。 四象限不是我发明的方法论，但「未完成任务自动跨日延续并计数」是我自己的设计决定。它来自一个具体观察：我用过的待办工具都以自然日为边界，而真正被拖延的事恰恰最容易被系统遗忘。这个判断没有 AI 能替我做——它来自我自己被拖延困扰的真实经验。
定义什么叫「做对了」。 这是我投入时间最多的部分。比如列表视图的排序规则最终定为「未完成优先 → 象限序号 → 拖延天数」，中间改过几版；「已拖 N 天」超过 3 天才高亮，这个阈值也是试出来的。AI 能很快给我一个能跑的排序函数，但哪种排序才是对的，得我自己用几天再回来判断。
验收。 我是这个应用的唯一用户，所以每一次改动都直接在自己的日常使用里验证。不好用我立刻知道。
AI 做的部分
具体的实现：JavaScript 逻辑、CSS 样式、Rust 侧的托盘与全局快捷键、Tauri 的打包配置。这些我能读懂、能判断改得对不对、能描述我想要什么，但让我从空白文件手写出来，我做不到。
几个技术选择，以及为什么
选择	理由
前端不用框架，单文件 index.html	这是一个只有我一个人用、功能边界清晰的小工具。引入框架会带来构建步骤和依赖管理，而这些成本换不来任何我需要的东西。能不加的复杂度就不加。
Tauri v2 而不是 Electron	常驻托盘的工具会一直挂在后台，体积和内存占用是真实成本。Tauri 用系统自带的 WebView，产物比 Electron 小一个量级。
localStorage，无后端无账号	待办清单是很私人的东西。不做后端不只是图省事，也意味着数据不出本机，我不需要为别人的隐私负责。
关闭按钮改为隐藏到托盘	常驻工具被点关闭时，用户想要的几乎从来不是「退出」。
我从这个过程里得到的
最大的收获不是「我会做 App 了」，而是搞清楚了一件事：当实现环节的成本被大幅压低之后，稀缺的能力就转移到了判断力——想清楚要做什么、说清楚要做成什么样、以及能不能验证它真的做到了。
这恰好是我原本的训练（法律训练本质上就是定义规则、界定边界、验证事实）能迁移过来的地方。做完这个项目之后，我把它当成了自己转向产品方向的起点，而不是一次性的尝试。

这一节描述的是真实的开发过程。如果你在评估我的技术能力，请以此为准：我能定义产品、能读懂并判断实现、能独立完成从设计到分发的全流程；我不能声称自己可以脱离 AI 工具手写等价的代码。
